# AI 활용 증빙

## 1. 개요

Begin Again 팀은 서비스 기획, 데이터 구조 설계, 프론트엔드·백엔드 구현, 오류 분석, AI 프롬프트 설계, 테스트 및 제출 문서 작성 과정에서 생성형 AI와 코딩 에이전트를 활용했습니다.

AI는 다음 두 가지 방식으로 활용했습니다.

1. **개발 보조 도구**

   * 서비스 기획 구체화
   * 코드 작성 및 오류 분석
   * 데이터 흐름 점검
   * 문서 작성 및 제출 준비

2. **서비스 핵심 기능**

   * 사용자 조건을 반영한 미션 추천
   * 완료된 여정 기록을 기반으로 한 에세이 생성
   * 네 컷 콘텐츠 및 카드뉴스용 문구 구조화

AI가 제안한 코드나 문서를 그대로 적용하지 않았습니다. 팀원이 요구사항과 실제 코드를 직접 비교하고, TypeScript 정적 검사와 사용자 흐름 테스트를 거쳐 검증된 결과만 프로젝트에 반영했습니다.

---

## 2. 활용한 AI 및 도구

| 구분     | 도구                | 활용 내용                                                   |
| ------ | ----------------- | ------------------------------------------------------- |
| 개발 보조  | ChatGPT           | 서비스 기획, 기능 우선순위, 오류 분석, 데이터 흐름 점검, 문서 작성                |
| 개발 보조  | 코딩 에이전트           | Expo·React Native 구현 보조, 반복적인 타입 수정, 리팩터링, Git 작업 절차 보조 |
| 서비스 기능 | Upstage Solar API | 미션 추천, 최종 에세이 생성, 시각 콘텐츠 문구 구조화                         |

### ChatGPT

* 서비스 문제 정의와 사용자 흐름 구체화
* 기능 구현 우선순위 정리
* TypeScript 및 런타임 오류 원인 분석
* Supabase 테이블·RPC·Edge Function 흐름 점검
* Git 브랜치, 병합 및 충돌 해결 절차 정리
* README와 제출 문서 작성 보조
* 테스트 시나리오와 보안 체크리스트 정리

### 코딩 에이전트

* Expo 및 React Native 코드 구현 보조
* 반복적인 타입 오류 수정
* 컴포넌트 및 서비스 로직 정리
* 코드 중복과 불필요한 상태 처리 점검
* Git 명령어와 병합 절차 안내
* Expo SDK 54 환경에 맞는 코드 검토

### Upstage Solar API

* 사용자 관심사와 조건을 반영한 미션 생성
* 완료된 여정 기록을 기반으로 한 에세이 작성
* 기록 간의 흐름을 연결하는 문장 생성
* 네 컷 콘텐츠 및 카드뉴스용 문구 구조화

---

## 3. 개발 단계별 AI 활용

## 3.1 문제 정의와 서비스 기획

AI와의 대화를 통해 초기 아이디어를 실제 서비스 구조로 구체화했습니다.

주요 활용 내용은 다음과 같습니다.

* 성과와 효율 중심의 일상에서 사용자가 자신을 충분히 돌아보지 못한다는 문제 정의
* 사용자가 부담 없이 참여할 수 있는 1주, 2주, 한 달 단위의 여정 설계
* 일상에서 바로 수행할 수 있는 일상형 미션 설계
* 사용자 주변 지역 공간을 활용한 장소형 미션 설계
* 긍정적인 감정뿐 아니라 불편함과 부정적인 감정도 기록할 수 있는 방식 검토
* 미션 수행 기록을 최종 에세이로 아카이빙하는 사용자 흐름 구성
* 서비스명 `Begin Again`과 브랜드 메시지 정리

AI가 제안한 아이디어 중 구현 기간과 사용자 경험에 적합한 항목만 팀 회의를 거쳐 채택했습니다.

---

## 3.2 데이터베이스 및 백엔드

AI를 활용해 Supabase 기반 데이터 구조와 요청 흐름을 설계하고 점검했습니다.

주요 활용 내용은 다음과 같습니다.

* 사용자와 프로필 데이터 구조
* 여정과 여정 기간 데이터 구조
* 미션과 미션 수행 기록 구조
* 사진, 감정, 메모 데이터 연결 방식
* 에세이 초안과 최종본 데이터 구조
* Supabase Auth 연동 흐름
* RPC 기반 여정 및 에세이 생성 과정
* Supabase Storage 사진 업로드 과정
* Edge Function 요청 및 응답 형식
* 사용자 소유 데이터에 대한 접근 흐름
* RLS 적용 시 발생할 수 있는 권한 문제 검토

AI의 제안은 실제 데이터베이스 스키마와 RPC 동작을 기준으로 다시 검토했습니다. 데이터 구조나 함수 이름이 실제 프로젝트와 일치하지 않을 경우 그대로 사용하지 않고 현재 코드에 맞게 수정했습니다.

---

## 3.3 AI 미션 추천

사용자의 관심사와 현재 여정 상태를 바탕으로 실천 가능한 미션을 추천하도록 프롬프트와 응답 형식을 설계했습니다.

### 주요 설계 기준

* 사용자 입력이 부족한 경우 잘못된 추천을 생성하지 않도록 처리
* 추상적인 조언보다 실제로 수행할 수 있는 구체적인 행동 제시
* 사용자의 관심사, 비용, 거리, 장소 조건 등을 반영
* 일상형 미션과 장소형 미션을 구분
* 사용자가 제공하지 않은 개인 정보나 경험을 임의로 생성하지 않도록 제한
* Edge Function 호출 실패 시 사용자에게 이해 가능한 오류 메시지 제공
* 응답 필드가 누락되거나 잘못된 경우 예외 처리

### 관련 코드

```text
services/upstage.ts
supabase/functions/
```

---

## 3.4 AI 에세이 생성

완료된 여정의 기록을 시간순으로 전달하고, 사용자가 실제로 기록한 경험만을 바탕으로 에세이를 생성하도록 구현했습니다.

에세이 생성 과정에서는 다음 정보를 활용합니다.

* 미션 제목과 내용
* 수행 날짜
* 사용자가 작성한 메모
* 선택한 감정
* 기록 사진과 관련 정보
* 여정 내 기록 순서

### 주요 제약 조건

* 사용자가 기록하지 않은 장소를 추가하지 않음
* 사용자가 기록하지 않은 사건을 생성하지 않음
* 사용자가 표현하지 않은 감정을 사실처럼 단정하지 않음
* 기록 순서와 `recordIndex`를 유지
* 입력된 기록 수와 출력 결과 수를 일치시킴
* 필수 필드가 없는 응답은 정상 결과로 저장하지 않음

### AI와 함께 분석한 대표 문제

* 기록 수와 AI 응답 배열 수가 일치하지 않는 문제
* `bridges` 배열의 항목이 예상보다 적게 반환되는 문제
* AI 응답이 올바른 JSON 형식을 따르지 않는 문제
* 필수 필드가 누락된 응답을 처리하는 문제
* 한 달 여정처럼 기록 수가 많을 때 순서가 변경되는 문제
* `recordIndex`가 누락되거나 중복되는 문제
* 사용자가 작성하지 않은 장소·감정·사건이 추가되는 환각 문제
* 에세이 저장 이후 최종본을 안정적으로 다시 조회하는 문제

### 관련 코드

```text
services/essay.service.ts
supabase/functions/generate-essay/
backend/scripts/
```

---

## 3.5 프론트엔드와 사용자 경험

AI 코딩 보조를 통해 다음 화면과 상호작용을 구현하고 개선했습니다.

* 회원가입 및 로그인
* 사용자 관심사 설정
* 여정 시작
* 추천 미션 확인
* 미션 수행 및 기록
* 사진, 감정, 메모 저장
* 캘린더 기반 기록 확인
* 다른 사용자의 공개 기록 탐색
* 에세이 책장
* 최종 에세이 상세 화면
* 네 컷 형태의 시각 콘텐츠
* 에세이 저장 및 확정 상태
* 저장이 완료된 에세이의 수정 제한
* 모바일 및 웹 환경의 반응형 UI

### 대표적인 오류 해결 사례

#### `ComicPanel` 타입 불일치

UI에서 사용하는 속성과 `ComicPanel` 타입 정의가 일치하지 않아 TypeScript 오류가 발생했습니다.

AI를 통해 오류가 발생한 타입 참조 위치를 확인했으며, 실제 UI 데이터 구조와 타입 정의를 비교한 뒤 팀원이 타입을 수정하고 `npx tsc --noEmit`으로 검증했습니다.

#### `undefined.map` 런타임 오류

배열로 예상한 데이터가 초기 렌더링 시 `undefined` 상태여서 `.map()` 호출 과정에서 오류가 발생했습니다.

AI를 활용해 데이터 초기화, 비동기 로딩 순서, 빈 배열 기본값을 점검했습니다. 수정 후 실제 화면 진입과 데이터 로딩 과정을 반복 테스트했습니다.

#### Expo 웹 흰 화면

웹 실행 시 화면이 렌더링되지 않는 문제에 대해 세션 초기화와 인증 상태 로딩 흐름을 점검했습니다.

AI가 제안한 원인 후보를 바탕으로 팀원이 브라우저 콘솔과 실행 로그를 확인하고, 실제 오류가 발생한 코드 경로를 수정했습니다.

#### Git 병합 충돌

`git pull`, `git switch`, `git merge` 과정에서 로컬 변경사항과 원격 브랜치가 충돌하는 상황이 발생했습니다.

AI를 활용해 `commit`, `stash`, `merge`, `abort`의 차이를 확인했으며, 팀원이 변경사항 보존 여부를 판단한 후 적절한 명령을 실행했습니다.

---

## 3.6 문서와 제출 준비

AI를 활용해 다음 내용을 정리하고 검토했습니다.

* Expo SDK 54 기준 실행 환경
* Node.js 버전 안내
* 저장소 클론 및 로컬 실행 방법
* 환경변수 설정 방법
* 포크 저장소 사용 규칙
* 배포 주소와 외부 서비스 의존성
* 심사용 사용자 흐름
* 시연 영상 진행 순서
* API 키와 환경변수 보안 점검
* README 및 AI 활용 증빙 문서 작성

문서의 기술 정보는 실제 `package.json`, `app.json`, 환경변수 이름과 실행 명령을 기준으로 팀원이 다시 확인했습니다.

---

## 4. AI 결과 검증 절차

AI가 생성하거나 수정한 결과는 다음 절차를 통해 검증했습니다.

## 4.1 정적 검사

```bash
npx tsc --noEmit
npm run lint
```

TypeScript 오류는 모두 해결했으며, ESLint 오류와 경고는 각각의 원인을 확인했습니다. 기능에 영향을 주는 오류는 수정한 뒤 최종 결과에 반영했고, 남은 경고가 있는 경우 그 영향 범위를 직접 검토했습니다.


## 4.2 실행 검사

```bash
npm run web
npm run start
```

웹 환경과 Expo 개발 서버에서 화면이 정상적으로 실행되는지 확인했습니다.

## 4.3 사용자 흐름 검사

다음 핵심 사용자 흐름을 직접 실행했습니다.

1. 회원가입 및 로그인
2. 관심사와 여정 기간 선택
3. 여정 생성
4. 추천 미션 조회
5. 미션 수행
6. 사진·감정·메모 저장
7. 캘린더 기록 조회
8. 다른 사용자의 공개 기록 탐색
9. AI 에세이 생성
10. 에세이 저장
11. 저장된 에세이 재조회
12. 오류 상황에서 사용자 안내 메시지 확인

## 4.4 백엔드 검사

* Supabase RPC 호출 결과 확인
* Edge Function 요청 성공 여부 확인
* Edge Function 응답 스키마 확인
* 필수 응답 필드 존재 여부 검사
* 입력 기록 수와 AI 결과 수 비교
* `recordIndex` 순서와 중복 여부 검사
* 테스트 계정을 활용한 실제 데이터 흐름 확인
* 사진 업로드와 데이터베이스 연결 상태 확인

---

## 5. AI 응답 실패에 대한 처리

생성형 AI의 응답은 항상 동일한 형식으로 반환된다고 보장할 수 없기 때문에 다음과 같은 실패 상황을 고려했습니다.

* 올바르지 않은 JSON 반환
* 필수 필드 누락
* 배열 길이 불일치
* 기록 순서 변경
* 네트워크 오류
* API 호출 실패
* 응답 시간 초과
* 사용자가 기록하지 않은 내용 생성

잘못된 응답은 그대로 저장하지 않고, 응답 검증 또는 예외 처리를 통해 사용자에게 다시 시도할 수 있도록 안내하는 방향으로 구현했습니다.

---

## 6. 개인정보 및 보안 고려

AI 기능을 구현하는 과정에서 다음 원칙을 적용했습니다.

* GitHub에 실제 비밀키를 커밋하지 않음
* Upstage API Key를 클라이언트 코드에 포함하지 않음
* 서버 전용 키는 Supabase Edge Functions 또는 백엔드 환경에 저장
* 클라이언트에는 Supabase publishable 또는 anon key만 사용
* AI 요청에는 기능 수행에 필요한 기록만 포함
* 사용자가 작성하지 않은 개인정보를 추론하도록 요청하지 않음
* 공개 범위가 설정된 기록은 해당 정책에 따라 처리
* 심사용 키는 대회 종료 후 폐기하도록 관리

---

## 7. 사람의 최종 책임

AI는 프로젝트의 기획과 구현을 보조하는 도구로 사용했습니다.

다음 사항은 팀원이 직접 판단하고 수행했습니다.

* 서비스 문제와 핵심 가치 결정
* 기능 범위와 개발 우선순위 결정
* AI가 제안한 코드 검토
* 데이터베이스 구조 결정
* API 요청 및 응답 구조 검토
* 기능 요구사항과 실제 구현의 일치 여부 판단
* 실행 오류 재현 및 수정
* 프롬프트와 AI 응답 품질 검토
* 최종 UI와 사용자 경험 결정
* 데이터와 API 키 보안 확인
* Git 커밋과 Pull Request 검토
* 배포 상태와 제출 결과 확인

AI의 제안이 실제 프로젝트 구조와 맞지 않거나 안정성을 보장하기 어려운 경우에는 적용하지 않았습니다.

---

## 8. 코딩 에이전트 작업 규칙

코딩 에이전트가 잘못된 저장소에 Pull Request, 이슈 또는 커밋을 생성하지 않도록 `AGENTS.md`에 다음 원칙을 명시했습니다.

> PR, 이슈, 커밋은 원본 레포지토리가 아닌 팀에서 포크한 레포지토리에 생성한다.

또한 Expo 관련 코드는 프로젝트에서 사용 중인 **Expo SDK 54** 기준의 공식 문서와 호환성을 바탕으로 검토하도록 제한했습니다.

관련 문서:

```text
AGENTS.md
```

---

## 9. AI 활용 관련 코드 위치

| 기능                   | 관련 위치                                |
| -------------------- | ------------------------------------ |
| AI 미션 추천 요청          | `services/upstage.ts`                |
| 미션 추천 Edge Function  | `supabase/functions/`                |
| 에세이 생성 서비스           | `services/essay.service.ts`          |
| 에세이 생성 Edge Function | `supabase/functions/generate-essay/` |
| 백엔드 통합 테스트           | `backend/scripts/`                   |
| 코딩 에이전트 규칙           | `AGENTS.md`                          |
| 프로젝트 실행 및 검증         | `README.md`                          |

---

## 10. 결론

Begin Again에서 AI는 사람의 경험을 대신 만들어내는 도구가 아니라, 사용자가 직접 남긴 기록을 발견하고 연결하며 돌아볼 수 있도록 돕는 보조 수단으로 활용했습니다.

개발 과정에서도 AI의 결과를 그대로 신뢰하지 않고, 팀원의 코드 검토, 정적 검사, 실제 실행, 사용자 흐름 테스트를 통해 최종 결과를 결정했습니다.
